# Optional: Placeholder for future custom IAM roles (e.g., for Velero, External Secrets)
# Core EKS IAM is handled automatically by the EKS module when enable_irsa = true

# Example: Velero IRSA will be added post-cluster creation if needed